fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Only needed when QUETZEL_PROVIDER=k8s (real cluster). Safe to install always.
kubernetes==31.0.0
# WP-A: authentication
PyJWT==2.10.1
authlib==1.6.12
httpx==0.28.1
